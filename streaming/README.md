## Streaming

Two ingestion patterns demonstrated:

- `generate_sample_data.py` — generates batch CSV files for ADF SFTP ingestion
- `stream_fleet_telematics.py` — simulates real-time fleet telemetry via Azure Event Hubs
